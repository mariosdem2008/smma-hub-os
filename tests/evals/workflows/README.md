# Workflow Dataset (Template)

This folder holds placeholder workflow datasets for Phase 0 evaluation planning.

Files
- multistep_100.jsonl (100 rows; template)

Notes
- Replace placeholders with real workflow traces before enabling eval runs.
- Enforce tenant scoping with 0 cross-tenant leaks in all fixtures.
