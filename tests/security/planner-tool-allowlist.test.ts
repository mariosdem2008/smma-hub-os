import { describe, it, expect } from "vitest";

describe.skip("tenant scoping: 0 cross-tenant leaks - planner tool allowlist", () => {
  it("placeholder", () => {
    expect(true).toBe(true);
  });
});
